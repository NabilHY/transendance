groups:
  - name: basic-health
    rules:
      - alert: ServiceDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: '{{ $labels.instance }} is down'
          description: '{{ $labels.job }} target has not responded for 1 minute.'